import styled from 'styled-components';

export const Wrapper = styled.div`
  box-sizing: border-box;
  margin: 20px;
  position: relative;
  width: calc(100% - 40px);
`;

export default {
  Wrapper,
}
